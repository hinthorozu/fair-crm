export interface TemplateContentTag { id: string; name: string; created_at: string }
export interface TemplateContent { id: string; tag_id: string; tag_name: string; title: string; created_at: string }
