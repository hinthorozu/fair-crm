{
  "version": 1,
  "id": "adapter-toggle-action-permission-consistency",
  "title": "Adapter Toggle Action Permission Consistency",
  "feature_type": "ui_only",
  "owner": "fair-crm",
  "platform_reusability": "existing_core_capability",
  "trigger": "user",
  "tenant_scope": "organization",
  "user_facing": true,
  "affected_paths": [
    "frontend/src/pages/AdapterManagementPage.tsx",
    "frontend/src/permissions/AdapterManagementPagePermissions.test.ts",
    ".kyrox/features/adapter-toggle-action-permission-consistency.json"
  ],
  "permissions": {
    "required": true,
    "items": [
      {
        "code": "fair_crm.scraper.update",
        "action": "activate or deactivate an adapter from Adapter Management",
        "scope": "organization",
        "assignable": true
      }
    ]
  },
  "frontend": {
    "required": true,
    "routes": ["/data-integration/adapters"],
    "menu": false,
    "menu_permission": null
  },
  "applicability": {
    "database": { "status": "na", "reason": "No persistence changes are introduced." },
    "migration": { "status": "na", "reason": "The existing fair_crm.scraper.update permission is reused." },
    "permissions": { "status": "required" },
    "backend": { "status": "na", "reason": "Existing scraper adapter activate/deactivate authorization remains unchanged and authoritative." },
    "api": { "status": "na", "reason": "No server API contract changes; only the Adapter Management toggle action is aligned with existing authorization." },
    "tenant_isolation": { "status": "na", "reason": "No organization-scoping behavior changes." },
    "audit": { "status": "na", "reason": "Existing adapter activation behavior is unchanged." },
    "frontend": { "status": "required" },
    "ui_permissions": { "status": "required" },
    "menu": { "status": "na", "reason": "No navigation menu changes are introduced." },
    "route_guard": { "status": "na", "reason": "The /data-integration/adapters route still requires fair_crm.scraper.read; this change only gates update mutations." },
    "forms": { "status": "na", "reason": "No form behavior changes." },
    "visual_qa": { "status": "required" },
    "backend_tests": { "status": "na", "reason": "Backend authorization behavior is unchanged." },
    "frontend_tests": { "status": "required" },
    "runtime_authorization": { "status": "required" },
    "runtime_verification": { "status": "required" },
    "deployment": { "status": "required" }
  },
  "notes": "AdapterManagementPage previously rendered and invoked activate/deactivate for users with only scraper read access, while the backend requires fair_crm.scraper.update. The toggle entry point now hides and fails closed without update permission."
}
