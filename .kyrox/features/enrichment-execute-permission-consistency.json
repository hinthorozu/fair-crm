{
  "version": 1,
  "id": "enrichment-execute-permission-consistency",
  "title": "Enrichment Execute Permission Consistency",
  "feature_type": "ui_only",
  "owner": "fair-crm",
  "platform_reusability": "existing_core_capability",
  "trigger": "user",
  "tenant_scope": "organization",
  "user_facing": true,
  "affected_paths": [
    "frontend/src/components/scraper/EnrichmentRunPanel.tsx",
    "frontend/src/components/scraper/EnrichmentStateResetPanel.tsx",
    "frontend/src/permissions/EnrichmentExecutePermissions.test.ts",
    ".kyrox/features/enrichment-execute-permission-consistency.json"
  ],
  "permissions": {
    "required": true,
    "items": [
      {
        "code": "fair_crm.scraper.execute",
        "action": "run customer contact enrichment and reset enrichment scan state",
        "scope": "organization",
        "assignable": true
      }
    ]
  },
  "frontend": {
    "required": true,
    "routes": ["/data-integration/adapters/:adapterKey", "/fairs/:fairId"],
    "menu": false,
    "menu_permission": null
  },
  "applicability": {
    "database": { "status": "na", "reason": "No persistence changes are introduced." },
    "migration": { "status": "na", "reason": "The existing fair_crm.scraper.execute permission is reused." },
    "permissions": { "status": "required" },
    "backend": { "status": "na", "reason": "Existing scraper and fair enrichment authorization remains unchanged and authoritative." },
    "api": { "status": "na", "reason": "No server API contract changes; enrichment mutation actions are aligned with existing execute authorization." },
    "tenant_isolation": { "status": "na", "reason": "No organization-scoping behavior changes." },
    "audit": { "status": "na", "reason": "Existing enrichment execution behavior is unchanged." },
    "frontend": { "status": "required" },
    "ui_permissions": { "status": "required" },
    "menu": { "status": "na", "reason": "No navigation menu changes are introduced." },
    "route_guard": { "status": "na", "reason": "Read-only enrichment views remain readable; only execute mutations are gated." },
    "forms": { "status": "required" },
    "visual_qa": { "status": "required" },
    "backend_tests": { "status": "na", "reason": "Backend authorization behavior is unchanged." },
    "frontend_tests": { "status": "required" },
    "runtime_authorization": { "status": "required" },
    "runtime_verification": { "status": "required" },
    "deployment": { "status": "required" }
  },
  "notes": "EnrichmentRunPanel and EnrichmentStateResetPanel previously exposed execute mutations to users with read-only scraper access. Both org-wide and fair-scoped enrichment runs, plus enrichment-state reset, are authorized by fair_crm.scraper.execute in the backend; the UI now hides and fails closed without execute permission."
}
