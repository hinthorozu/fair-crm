{
  "version": 1,
  "id": "follow-ups-save-handler-permission-consistency",
  "title": "Follow-ups Save Handler Permission Consistency",
  "feature_type": "ui_only",
  "owner": "fair-crm",
  "platform_reusability": "existing_core_capability",
  "trigger": "user",
  "tenant_scope": "organization",
  "user_facing": true,
  "affected_paths": [
    "frontend/src/pages/FollowUpsPage.tsx",
    "frontend/src/permissions/FollowUpsSavePermissions.test.ts",
    ".kyrox/features/follow-ups-save-handler-permission-consistency.json"
  ],
  "permissions": {
    "required": true,
    "items": [
      {
        "code": "fair_crm.todos.update",
        "action": "record follow-up worklist activity",
        "scope": "organization",
        "assignable": true
      }
    ]
  },
  "frontend": {
    "required": true,
    "routes": ["/todos"],
    "menu": false,
    "menu_permission": null
  },
  "applicability": {
    "database": { "status": "na", "reason": "No persistence changes are introduced." },
    "migration": { "status": "na", "reason": "The existing fair_crm.todos.update permission is reused." },
    "permissions": { "status": "required" },
    "backend": { "status": "na", "reason": "Existing worklist activity authorization remains unchanged and authoritative." },
    "api": { "status": "na", "reason": "No server API contract changes." },
    "tenant_isolation": { "status": "na", "reason": "No organization-scoping behavior changes." },
    "audit": { "status": "na", "reason": "Existing worklist activity auditing remains unchanged." },
    "frontend": { "status": "required" },
    "ui_permissions": { "status": "required" },
    "menu": { "status": "na", "reason": "No navigation menu changes are introduced." },
    "route_guard": { "status": "na", "reason": "Follow-up read behavior is unchanged; only the mutation handler is hardened." },
    "forms": { "status": "required" },
    "visual_qa": { "status": "required" },
    "backend_tests": { "status": "na", "reason": "Backend authorization behavior is unchanged." },
    "frontend_tests": { "status": "required" },
    "runtime_authorization": { "status": "required" },
    "runtime_verification": { "status": "required" },
    "deployment": { "status": "required" }
  },
  "notes": "The shared worklist modal already gates activity submission with fair_crm.todos.update. FollowUpsPage also calls the mutation API directly in its parent save callback, so that handler now independently fails closed when update permission is absent."
}
